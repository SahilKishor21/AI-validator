[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "backend"
source = "backend"

[[services]]
name = "frontend"
source = "frontend"

[[services]]
name = "database"
image = "postgres:15"